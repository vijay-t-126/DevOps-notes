1)count how many users logged in
#!/bin/bash

count= $(who | wc -l)
echo "no of users logged in: $count"
=====================
2)factorial for given set of numbers 12345
#!/bin/bash
input = "1 2 3 4 5"
for i in $input; do
	fact=1
	num=$i
	while [$num -gt 0]; do
		fact=`expr $fact \* $num`
		num=`expr $num - 1`
	done
	echo "factorial of $i is $fact"
done
==========================		
3)check if user is exist
#!/bin/bash
echo "enter the user name"
read user
if [ getent passwd | grep -qi "$user" ]; then
# if getent passwd "$user" > /dev/null ; then
	echo "$user exist"
else
	echo "$user does not exist"
fi
=========================
4)rename .c to .txt
#!/bin/bash

find <dir> -type f -name "*.c" > temp
while read line
do 
	new_name= "echo $line | sed s/.c/.txt/ig'
	mv $line $new_name
done < temp
rm temp
============================
1)what are different status codes
2)Git architecture
3)how to push empty directory
4)how to undo commit changes without tracking history
5)git stash
6)difference between merge and rebase
7)difference between revert and reset
8)how to push all tags
9)difference between maven and gradle
10)mvn clean install
11) difference between jar,war and ear
12)difference between git add .,git add *, git add -A
13)how to get the tags from remote to local
14)how to exclude one file during git add
15)how to change port number for tomcat
16)stickybit
17)how to see all process with cpu usage
18)how to clone one particular branch from remote repo to local
19)maven lifecycle
20)git squash



